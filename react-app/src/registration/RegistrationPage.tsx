import RegistrationForm from "@/registration/RegistrationForm.tsx";
import {Container} from "@mui/material";

const RegistrationPage = () => {
    return <Container>
        <RegistrationForm  onAction={() => {}}/>
    </Container>
}

export default RegistrationPage;